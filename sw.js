// This is a basic Service Worker for Paschala Hub
self.addEventListener('install', (e) => {
  console.log('SW Installed');
});

self.addEventListener('fetch', (e) => {
  // Keeps the app running smoothly
});

